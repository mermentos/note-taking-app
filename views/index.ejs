<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - NoteMaster</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <div class="form-container">
            <h1><%= showSignup ? 'Sign Up' : 'Login' %></h1> <!-- Toggle based on signup or login -->
            <form action="<%= showSignup ? '/signup' : '/login' %>" method="POST">
                <input type="text" name="username" placeholder="Username" required>
                <input type="password" name="password" placeholder="Password" required>
                <button type="submit"><%= showSignup ? 'Sign Up' : 'Login' %></button>
            </form>
            <div class="form-switch" onclick="window.location.href='/?signup=<%= showSignup ? 'false' : 'true' %>'">
                <%= showSignup ? 'Already have an account? Log in here!' : 'Don\'t have an account? Sign up here!' %>
            </div>
        </div>
    </div>

    <% if (loginFailed) { %>
    <div id="login-failed-modal" class="modal show-modal">
        <div class="modal-content">
            <span class="close" onclick="closeLoginFailedModal()">&times;</span>
            <h2>Oops! Something went wrong 😕</h2>
            <p>The username or password you entered is incorrect. Please try again, or <a href="/?signup=true">sign up</a> for an account.</p>
            <button onclick="closeLoginFailedModal()">Try Again</button>
        </div>
    </div>
    <% } %>

    <script>
        function closeLoginFailedModal() {
            document.getElementById('login-failed-modal').style.display = 'none';
        }

        window.onload = function() {
            if (document.getElementById('login-failed-modal')) {
                document.getElementById('login-failed-modal').style.display = 'block';
            }
        };
    </script>
</body>
</html>
